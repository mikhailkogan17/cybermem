apiVersion: v1
kind: Secret
metadata:
  name: "{{ .Chart.Name }}-api-key"
  labels:
    app.kubernetes.io/name: "{{ .Chart.Name }}"
type: Opaque
stringData:
  om_api_key: "{{ .Values.env.OPENMEMORY_API_KEY }}"
