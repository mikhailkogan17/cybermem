name: E2E Testing

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:
  workflow_call:

jobs:
  e2e:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Setup Environment
        run: |
          mkdir -p ~/.cybermem
          echo "OPENAI_API_KEY=sk-test-key-for-ci" > ~/.cybermem/.env # gitleaks:allow
          echo "OM_API_KEY=sk-test-key-for-ci" >> ~/.cybermem/.env # gitleaks:allow

      - name: Log in to the Container registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Start Docker Stack (Local Simulation)
        working-directory: packages/cli/templates
        run: |
          docker version
          docker compose version
          # Override arm64 platform for CI (Ubuntu runs amd64)
          export DOCKER_DEFAULT_PLATFORM=linux/amd64
          export CYBERMEM_ENV_PATH=$HOME/.cybermem/.env
          docker compose up -d
          docker compose ps
          sleep 60 # Wait for services to be ready

      - name: Install CLI Dependencies
        working-directory: packages/cli
        run: npm ci

      - name: Run E2E Tests (MCP)
        working-directory: packages/cli
        env:
          OM_API_KEY: sk-test-key-for-ci # gitleaks:allow
        run: npm run test:e2e local

      - name: Verify MCP STDIO Protocol
        run: |
          cd packages/mcp
          npm ci --silent
          npm run build --silent
          echo '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2024-11-05","clientInfo":{"name":"ci-stdio-test","version":"1.0"},"capabilities":{}}}' | \
          node dist/index.js 2>&1 | grep -q '"serverInfo"' || (echo "❌ MCP STDIO Protocol Check Failed" && exit 1)
          echo "✅ MCP STDIO Protocol Verified"

      - name: Install Dashboard Dependencies
        working-directory: packages/dashboard
        run: npm ci

      - name: Install Playwright Browsers
        working-directory: packages/dashboard
        run: npx playwright install --with-deps chromium

        env:
          CI: true
          OM_API_KEY: sk-test-key-for-ci # gitleaks:allow
          DB_EXPORTER_URL: http://127.0.0.1:8000
          PROMETHEUS_URL: http://127.0.0.1:9092
          NEXT_PUBLIC_PROMETHEUS_URL: http://127.0.0.1:9092

      - name: Wait for Exporters (Health Check)
        run: |
          echo "Waiting for DB Exporter..."
          timeout 30s bash -c 'until curl --silent --fail http://127.0.0.1:8000/api/stats; do sleep 2; done'
          echo "DB Exporter is UP"

      - name: Run Playwright Tests (Dashboard)
        working-directory: packages/dashboard
        run: npm run test:e2e -- e2e/auth.spec.ts e2e/metrics.spec.ts
        env:
          CI: true
          OM_API_KEY: sk-test-key-for-ci # gitleaks:allow
          DB_EXPORTER_URL: http://127.0.0.1:8000
          PROMETHEUS_URL: http://127.0.0.1:9092
          NEXT_PUBLIC_PROMETHEUS_URL: http://127.0.0.1:9092
          BASE_URL: http://127.0.0.1:3000

      - name: Wipe Database (Cleanup)
        if: always()
        run: |
          # Direct docker cleanup - more reliable than ts-node in CI
          docker exec cybermem-mcp sh -c 'rm -f /data/openmemory.sqlite*' || true
          docker restart cybermem-mcp || true
          echo "✅ Database wiped"

      - name: Upload Playwright Report
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: packages/dashboard/playwright-report/
          retention-days: 7
