---
description: Run Playwright E2E tests for local environment (localhost:3000 dashboard, localhost:8626 API)
---

# Local E2E Tests

// turbo-all

1. Run local tests:
```bash
cd packages/cli && npm run test:e2e local
```
